#include <stdio.h>
#include <math.h>
int main()
{
    int s, n[6], x;

    for (int i = 0; i < 6; i++)
    {

        printf("ente nombre :");
        scanf("%d", &n[i]);
    }
    for (int i = 0; i < 6; i--)
    {

        printf("ente nombre :");
        scanf("%d", &n[i]);
    }
}